package util;

public class Config {

    // Specify the file path for the data files to test
    public static final String FULL_FILE_PATH = "...";

    // Specify which index to test (1, 2, 3, 4, 5a, 5b, 5c, 5d, 6a, 6b)
    public static final String INDEX_TO_TEST = "1";
}
